## Date Definitions

**Week to Date (WTD):** When the user asks for "week to date" or "WTD", always define the period as the start of the current week through **yesterday** (i.e., exclude today). Do not include today's partial data in WTD calculations.
